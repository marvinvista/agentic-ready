# Documentation

## Quickstart

Use an API key or OAuth Bearer token. SDK client libraries are available for Python and TypeScript.

The sandbox and test mode return deterministic sample responses.

Versioning follows semver. Deprecation windows are 180 days. The changelog documents breaking changes.
